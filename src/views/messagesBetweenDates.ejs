<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Messages Between Dates</title>
</head>
<body>
    <h1>Messages Between Dates</h1>
    <ul>
        <% messages.forEach(message => { %>
            <li>
                <% if (message.user) { %>
                    <strong>User:</strong> <%= message.user.username %>
                <% } else { %>
                    <strong>User:</strong> Unknown
                <% } %>
                <br>
                <strong>Content:</strong> <%= message.content %>
            </li>
        <% }); %>
    </ul>
</body>
</html>

<!-- Input exemple {
    "startDate": "2024-04-01T00:00:00.000Z",
    "endDate": "2024-04-04T23:59:59.999Z"
} -->
